import { LoaderButton, LoaderContainer } from './Loader.styled';

export const Loader = () => {
  return (
    <LoaderContainer>
      <LoaderButton>Load more</LoaderButton>
    </LoaderContainer>
  );
};
